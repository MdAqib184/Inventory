{
  "name": "inventory-manager",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "cd client && npm run build",
    "package": "electron-packager . InventoryManager --platform=win32 --arch=x64 --out=dist --overwrite --ignore=node_modules/electron-* --prune=true"
  },
  "devDependencies": {
    "electron": "^35.2.1",
    "electron-packager": "^17.1.2"
  }
}
